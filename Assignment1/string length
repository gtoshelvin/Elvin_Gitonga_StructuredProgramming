#include <stdio.h>
#include <string.h>

int main() {
    char name[100];

    printf("Enter a string: ");
    scanf("%s", name);

    printf("You entered: %s\n", name);
    printf("Length of the string is: %lu\n", strlen(name));

    return 0;
}
